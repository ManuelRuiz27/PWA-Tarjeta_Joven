# Copia este archivo como .env.local y ajusta valores

# Base de la API (Nginx proxy /api -> backend)
VITE_API_BASE=/api

# Clave pública VAPID para notificaciones Web Push
# Genera desde tu backend o servicio de push
VITE_VAPID_PUBLIC_KEY=REEMPLAZAR_CON_CLAVE_VAPID_PUBLICA

# Sentry (opcional)
# VITE_SENTRY_DSN=https://<public_dsn>@oXXXX.ingest.sentry.io/XXXXX
